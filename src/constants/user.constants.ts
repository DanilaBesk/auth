import { Role } from '@prisma/client';

export const INITIAL_USER_ROLE = Role.user;

export const MAX_FIRST_NAME_LENGTH = 200;
export const MAX_LAST_NAME_LENGTH = 200;
export const MAX_EMAIL_LENGTH = 256;
